import wollok.game.*

import flappywolbird.*

program FlappyWolbird {
	game.title("FLappy wolbird")
	juego.configurar()
	// add assets in asset folder, for example, for the background
  // game.boardGround("fondo2.jpg")

	//

	//game.showAttributes(wolbird) //Debug

	game.start()
}
